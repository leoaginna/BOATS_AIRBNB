<%= simple_form_for(@boat) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price %>
  <%= f.input :address %>
  <%= f.input :photo, as: :file, input_html: { multiple: true } %>
  <%= f.button :submit, @boat.nil? ? "Create Boat" : "Update boat" %>
<% end %>
